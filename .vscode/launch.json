{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run reg.go",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/main.go",
            "args": ["basement.fan.enabled", "true"],
            "env": {
                "MQTT": "10.1.0.12"
            },
        }
    ]
}